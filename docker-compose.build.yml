# Build the image locally instead of pulling from GHCR:
#   docker compose -f docker-compose.yml -f docker-compose.build.yml up -d --build
services:
  studio-os:
    build:
      context: .
      dockerfile: Dockerfile
    image: studio-os:local
